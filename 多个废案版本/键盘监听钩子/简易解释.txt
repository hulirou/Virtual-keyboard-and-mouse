按照如下逻辑：

   硬件（键盘） → 内核层驱动 → 内核输入子系统 → 用户态驱动（可选） → 用户层 API → 应用程序
 
 
 
 在内核层驱动创建过滤器太费劲，需要给每个设备都加上过滤器太臃肿。他们一定有个可以接受输入后统一添加到列队中的管理机构，就是内核输入子系统。
 所以我根据AI的提示找到了一篇看起来能用的文章，在kbdclass.sys驱动的KeyboardClassServiceCallback函数，我只做了监听，我觉得甚至可以直接写入HID键盘输入。不过这个需要3换程序控制加入的具体按键